@page
@using Microsoft.AspNetCore.Http.Features
@model TwoFactorAuthenticationModel
@{
    ViewData["Title"] = "Two-factor authentication (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
@{
    var consentFeature = HttpContext.Features.Get<ITrackingConsentFeature>();
    @if (consentFeature?.CanTrack ?? true)
    {
        @if (Model.Is2faEnabled)
        {
            if (Model.RecoveryCodesLeft == 0)
            {
                <div class="alert alert-danger">
                    <strong>@Localiser["TwoFactor_NoCodesLeft"]</strong>
                    <p>@Html.Raw(string.Format(System.Globalization.CultureInfo.CurrentCulture, Localiser["TwoFactor_MustGenerate"].Value, $"<a href=\"{Url.Page("./GenerateRecoveryCodes")}\">{Localiser["TwoFactor_GenerateLink"].Value}</a>"))</p>
                </div>
            }
            else if (Model.RecoveryCodesLeft == 1)
            {
                <div class="alert alert-danger">
                    <strong>@Localiser["TwoFactor_OneCodeLeft"]</strong>
                    <p>@Html.Raw(string.Format(System.Globalization.CultureInfo.CurrentCulture, Localiser["TwoFactor_CanGenerate"].Value, $"<a href=\"{Url.Page("./GenerateRecoveryCodes")}\">{Localiser["TwoFactor_GenerateLink"].Value}</a>"))</p>
                </div>
            }
            else if (Model.RecoveryCodesLeft <= 3)
            {
                <div class="alert alert-warning">
                    <strong>You have @Model.RecoveryCodesLeft recovery codes left.</strong>
                    <p>@Html.Raw(string.Format(System.Globalization.CultureInfo.CurrentCulture, Localiser["TwoFactor_ShouldGenerate"].Value, $"<a href=\"{Url.Page("./GenerateRecoveryCodes")}\">{Localiser["TwoFactor_GenerateLink"].Value}</a>"))</p>
                </div>
            }

            if (Model.IsMachineRemembered)
            {
                <form method="post" style="display: inline-block">
                    <button type="submit" class="btn btn-primary">@Localiser["TwoFactor_ForgetBrowser"]</button>
                </form>
            }
            <a asp-page="./Disable2fa" class="btn btn-primary">@Localiser["TwoFactor_Disable"]</a>
            <a asp-page="./GenerateRecoveryCodes" class="btn btn-primary">@Localiser["TwoFactor_ResetRecoveryCodes"]</a>
        }

        <h4>@Localiser["TwoFactor_AuthenticatorApp"]</h4>
        @if (!Model.HasAuthenticator)
        {
            <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-primary">@Localiser["TwoFactor_AddApp"]</a>
        }
        else
        {
            <a id="enable-authenticator" asp-page="./EnableAuthenticator" class="btn btn-primary">@Localiser["TwoFactor_SetUpApp"]</a>
            <a id="reset-authenticator" asp-page="./ResetAuthenticator" class="btn btn-primary">@Localiser["TwoFactor_ResetApp"]</a>
        }
    }
    else
    {
        <div class="alert alert-danger">
            <strong>@Localiser["TwoFactor_PolicyNotAccepted"]</strong>
            <p>@Localiser["TwoFactor_MustAcceptPolicy"]</p>
        </div>
    }
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
